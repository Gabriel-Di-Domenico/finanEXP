import { ConvertEnumPipe } from './convert-enum.pipe';

describe('ConvertEnumPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertEnumPipe();
    expect(pipe).toBeTruthy();
  });
});
